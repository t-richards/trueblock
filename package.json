{
  "private": true,
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.0.10",
    "@types/chrome": "^0.1",
    "typescript": "^5.0"
  },
  "dependencies": {
    "@biomejs/biome": "2.3.3",
    "@picocss/pico": "^2.1",
    "@testing-library/preact": "^3.2.4",
    "@types/bun": "^1.3.1",
    "preact": "^10.27"
  },
  "scripts": {
    "build": "bun build --target browser --outdir dist --minify src/worker.ts src/popup.tsx src/options.tsx src/app.css",
    "build:dev": "bun build --target browser --outdir dist --sourcemap=inline src/worker.ts src/popup.tsx src/options.tsx src/app.css --watch",
    "clean": "rm -f dist/*.js dist/*.css dist/*.map dist/*.zip",
    "dist": "bun run clean && bun run build",
    "lint": "biome check --write",
    "lint:ci": "biome check",
    "pack": "cd dist && zip -9 -r trueblock.zip *",
    "tsc": "tsc --project tsconfig.json --noEmit --watch --preserveWatchOutput"
  }
}
