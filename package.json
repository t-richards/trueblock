{
    "private": true,
    "devDependencies": {
        "@types/chrome": "0.0.146"
    },
    "dependencies": {
        "esbuild": "^0.12.15",
        "milligram": "^1.4.1"
    },
    "scripts": {
        "build": "esbuild src/background.ts --bundle --minify --outfile=dist/background.js",
        "dist": "npm run clean && npm run build && npm run pack",
        "clean": "rm -f dist/*.js dist/*.zip",
        "pack": "cd dist && zip -r package.zip *"
    }
}
