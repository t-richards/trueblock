{
  "private": true,
  "dependencies": {
    "@picocss/pico": "^2.1.1",
    "preact": "^10.28.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@happy-dom/global-registrator": "^20.3.9",
    "@testing-library/preact": "^3.2.4",
    "@types/bun": "^1.3.6",
    "@types/chrome": "^0.1.36",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "bun build --target browser --outdir dist --minify src/worker.ts src/popup.tsx src/options.tsx src/app.css",
    "build:dev": "bun build --target browser --outdir dist --sourcemap=inline --watch src/worker.ts src/popup.tsx src/options.tsx src/app.css",
    "clean": "rm -f dist/*.js dist/*.css dist/*.map dist/*.zip",
    "dist": "bun run clean && bun run build",
    "lint": "biome check --write",
    "lint:ci": "biome check",
    "pack": "cd dist && zip -9 -r trueblock.zip *",
    "tsc:dev": "tsc --project tsconfig.json --noEmit --watch --preserveWatchOutput",
    "tsc": "tsc --project tsconfig.json --noEmit"
  }
}
